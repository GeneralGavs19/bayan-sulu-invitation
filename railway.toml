[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && echo 'Fresh build 04:07'"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/health"
restartPolicyType = "on-failure"
