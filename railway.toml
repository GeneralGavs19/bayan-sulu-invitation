[build]
builder = "nixpacks"
buildCommand = "echo 'Build refresh at 2026-04-11 03:59' && cd server && npm install"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/health"
restartPolicyType = "on-failure"
