[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && echo 'Build with delete-all v4:33'"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/health"
restartPolicyType = "on-failure"
